# Purpose  : grant CAP_NET_BIND_SERVICE so the non-privileged logstash user can bind the privileged syslog port UDP/514 (< 1024) without running the service as root (least-privilege principle).

[Service]
AmbientCapabilities=CAP_NET_BIND_SERVICE
